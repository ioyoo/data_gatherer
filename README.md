# ioyoo
Stock analyzer from social media

# create environment
python3 -m venv .env ---> root folder

source ./.env/bin/activate ---> terminal

# help with pytest
pytest --collect-only
pytest --pyargs mypkg